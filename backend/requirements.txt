flask==2.3.0
flask-cors==6.0.1
gunicorn==21.2.0
python-dotenv==1.0.0
requests==2.31.0
pillow==11.1.0
numpy>=1.23.5,<2.0.0
# Face recognition runs on OpenCV's built-in YuNet (detector) + SFace
# (recognizer) ONNX models. Both are bundled in opencv-python >= 4.5.4.
# We avoid TensorFlow / DeepFace entirely to keep the worker under 512 MB.
opencv-python>=4.5.5,<4.10
# bing-image-downloader is no longer required at runtime (we scrape the async
# endpoint directly in search_images.py), but kept as a safety fallback.
bing-image-downloader==1.1.2
# The `duckduckgo_search` package has been renamed to `ddgs` upstream.
ddgs>=9.0.0
gunicorn==21.2.0